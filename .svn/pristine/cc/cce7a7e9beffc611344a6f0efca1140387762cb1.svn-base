
v_businessable.sql
========================================

MySQL doesn't support syntax:

 CAST(name AS VARCHAR(150))


You have to use follow syntax for MySQL:

 CAST(name AS CHAR(150))
